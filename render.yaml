services:
  - type: web
    name: creocash-api
    runtime: node
    plan: starter
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: creocash-db
          property: connectionString

databases:
  - name: creocash-db
    databaseName: creocash
    plan: starter