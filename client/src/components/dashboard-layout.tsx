// Re-export the canonical layout so legacy `@/components/dashboard-layout`
// imports pick up the same cleaned-up sidebar everyone else uses.
export { DashboardLayout } from "@/features/dashboard/components/dashboard-layout";
